import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.mvfactory.dpf',
  appName: 'DPF',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
